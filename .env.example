# Gatehouse's own Supabase project. Do NOT point this at the RA-workspace
# project - Gatehouse is a standalone product for outside users and must not
# share auth/session infrastructure with RideArrivo's internal workspace data.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
